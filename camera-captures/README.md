# Camera captures

Recordings of Rocket League's own replay camera, made with the BakkesMod plugin in
`tools/camera-capture` (see its README).

Put capture `.csv` files here, with each replay saved as `<replay id>.replay` here or
in the project root, and run `npm run camera:compare`. Reports are written to
`reports/`, which is not committed.
